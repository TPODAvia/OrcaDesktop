logger:
  default: info

mqtt:
  enabled: false

detectors:
  onnx:
    type: onnx

model:
  model_type: yolo-generic
  width: 320          # matches IMG_SIZE we used above
  height: 320
  input_tensor: nchw
  input_dtype: float
  path: /config/models/yolov9-s-320.onnx   # <--- name from step 1
  labelmap_path: /labelmap/coco-80.txt

detect:
  enabled: true

objects:
  track:
    - person
    - car

ffmpeg:
  hwaccel_args: preset-nvidia-h264

cameras:
  cam1:
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsp://localhost:8554/yhs_yhsros2_cam1
          # roles: [detect, record]
          roles: [detect]
    detect:
      width: 1280
      height: 720
    record:
      enabled: true

  cam2:
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsp://localhost:8554/yhs_yhsros2_cam2
          roles: [detect]
    detect:
      width: 1280
      height: 720
    record:
      enabled: true

  cam3:
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsp://localhost:8554/yhs_yhsros2_cam3
          roles: [detect]
    detect:
      width: 1280
      height: 720
    record:
      enabled: true

  cam4:
    enabled: true
    ffmpeg:
      inputs:
        - path: rtsp://localhost:8554/yhs_yhsros2_cam4
          roles: [detect]
    detect:
      width: 1280
      height: 720
    record:
      enabled: true

record:
  enabled: true
  retain:
    days: 3
    mode: motion
